import { ShowDataPipePipe } from './show-data-pipe.pipe';

describe('ShowDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
